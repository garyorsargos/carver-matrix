import { Typography } from "@mui/material";

export const EditMatrix: React.FC = () => {
  return (
    <Typography data-testid="edit-page" sx={{ color: "#FFF" }}>
      This is the edit page. Congratulations!
    </Typography>
  );
};

export default EditMatrix;