import { Typography } from "@mui/material";

export const Register: React.FC = () => {
  return (
    <Typography data-testid="register-page" sx={{ color: "#FFF" }}>
      This is the Register page. Congratulations!
    </Typography>
  );
};

export default Register;
